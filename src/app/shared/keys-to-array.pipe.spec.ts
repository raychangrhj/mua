import { KeysToArrayPipe } from './keys-to-array.pipe';

describe('KeysToArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new KeysToArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
